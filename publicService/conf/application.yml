server:
  port: 9002
spring:
  application:
    name: cloud-publicservice

  mail:
    host: smtp.126.com
    port: 25
    username: huashuizhuhui@126.com
    password: zhuhui19870303
    nickname: 数星平台
    properties:
      smtp:
        starttls:
          enable: true
          required: true
        auth: true
      mail:
        smtp:
          ssl:
            enable: false

eureka:
  client:
    serviceUrl:
      defaultZone: locahost
  instance:
    metadata-map:
      test: wedatasphere

management:
  endpoints:
    web:
      exposure:
        include: refresh,info
logging:
  config: classpath:log4j2.xml

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: countSql